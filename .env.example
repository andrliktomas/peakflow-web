# Kam se odesílá kontaktní formulář (Formspree, Web3Forms, vlastní API…).
# Zkopírujte do .env.local pro lokální vývoj; v produkci nastavte
# v Cloudflare Pages → Settings → Variables and Secrets.
NEXT_PUBLIC_FORM_ENDPOINT=
